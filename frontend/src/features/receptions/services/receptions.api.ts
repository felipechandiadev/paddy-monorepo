import { ReceptionListItem, CreateReceptionPayload, Reception, UpdateReceptionPayload } from '../types/receptions.types';

const API_BASE_URL = `${process.env.NEXT_PUBLIC_API_URL}/operations/receptions`;

// Note: This file is mostly for documentation.
// Most operations are done through server actions in fetch.action.ts
// to ensure proper authentication handling.

export const receptionApi = {
  // API methods are handled through server actions for security
};
